{
  "name": "my-async-store",
  "version": "0.0.1",
  "description": "my-async-store",
  "main": "dist/my-async-store.cjs.js",
  "scripts": {
    "clean": "rm -rf dist/ types/",
    "build": "yarn clean && rollup -c",
    "test": "mocha --extension ts --require ts-node/register"
  },
  "author": "shadow",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "assert": "^2.0.0",
    "mocha": "^6.2.2",
    "rollup": "^1.25.2",
    "rollup-plugin-terser": "^5.1.2",
    "rollup-plugin-typescript2": "^0.24.3",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.4"
  }
}
